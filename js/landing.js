/**
 * landing.js — Landing page specific interactions
 */

(function () {
  "use strict";

  document.addEventListener("DOMContentLoaded", function () {
    /* Smooth scroll is handled via CSS scroll-behavior on html.
       No additional landing-specific JS needed at this phase. */
  });
})();
