/**
 * landing.js — Landing page specific interactions
 */

(function () {
  "use strict";

  document.addEventListener("DOMContentLoaded", function () {
    if (typeof RMSIcons !== "undefined") {
      RMSIcons.injectAll();
    }
  });
})();
